{
  "PostToolUse": [
    {
      "matcher": "Edit.*\\.rb$",
      "hooks": [
        {
          "type": "command",
          "command": "if command -v rubocop >/dev/null 2>&1; then bundle exec rubocop --auto-correct \"$FILEPATH\" 2>/dev/null || true; fi",
          "timeout": 30
        }
      ]
    },
    {
      "matcher": "Write.*\\.rb$",
      "hooks": [
        {
          "type": "command",
          "command": "if command -v rubocop >/dev/null 2>&1; then bundle exec rubocop --auto-correct \"$FILEPATH\" 2>/dev/null || true; fi",
          "timeout": 30
        }
      ]
    }
  ],
  "UserPromptSubmit": [
    {
      "matcher": ".*migrate.*",
      "hooks": [
        {
          "type": "command",
          "command": "echo '\n⚠️  Migration Safety Reminder:\n- Ensure migration is reversible\n- Check for data migration needs\n- Add indexes for foreign keys\n- Test rollback before deployment'",
          "timeout": 5
        }
      ]
    },
    {
      "matcher": ".*(deploy|production|push to prod).*",
      "hooks": [
        {
          "type": "command",
          "command": "echo '\n🚀 Production Deployment Checklist:\n- All tests passing?\n- Database migrations safe?\n- Environment variables configured?\n- Rollback plan ready?'",
          "timeout": 5
        }
      ]
    }
  ],
  "PreToolUse": [
    {
      "matcher": "Bash\\(rm:.*\\)",
      "hooks": [
        {
          "type": "command",
          "command": "echo '\n⚠️  File Deletion Warning: About to delete files. Ensure this is intentional.'",
          "timeout": 5
        }
      ]
    }
  ]
}
